
class TestStateMachine < Test::Unit::TestCase
  
  def setup
    @sm = StateMachine.new
  end
  
  def teardown
    @sm = nil
  end

  def test_state_machine
    assert_equal @sm.state == :prepare_message
# run tests if run locally 
if $0 == __FILE__
  require '../state_machine'
end
