require File.
  expand_path(File.
  join(File.
  dirname(__FILE__),'..','..','..','build','state','build_state_machine'))

describe 'StateMachine' do
  before(:each) do
    @s = BuildStateMachine.new('test_10_state','primary').state_machine
  end

  after(:each) do
    @s = nil
  end

  describe 'should be an instance of StateMachine' do
    it  do
      @s.class.should == StateMachine
    end
  end
end
